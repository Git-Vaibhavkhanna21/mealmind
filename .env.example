# This file is for local Python development only — api/ (agents, workflows,
# mcp_servers), scripts/, and database/seed_recipes.py all call
# load_dotenv() and read from a root-level .env when run outside Railway.
# Copy this file to .env and fill in real values:
#   cp .env.example .env
# frontend/ has its own separate example file (frontend/.env.local.example)
# for the Next.js app's environment variables — this one doesn't cover those.

# Used by database/seed_recipes.py and the Python MCP servers.
SUPABASE_URL=https://dssyufxseilflgmttavt.supabase.co
SUPABASE_SERVICE_KEY=
OPENAI_API_KEY=
THEMEALDB_API_KEY=1

# Used by agents/parser.py, agents/expiration.py, and the receipt parsing
# workflow (workflows/receipt_parsing.py) for Claude Haiku/Sonnet calls.
ANTHROPIC_API_KEY=
